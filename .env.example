# Copy this to .env and fill in your values for local development

# PostgreSQL connection string (required for the API server)
DATABASE_URL=postgresql://user:password@localhost:5432/frcscout

# Optional — defaults shown below if omitted
# PORT=5173          (frontend dev server)
# PORT=8080          (api server)
# API_URL=http://localhost:8080   (frontend proxy target)
# BASE_PATH=/
